# 0331：验证二叉树的前序序列化（★★）


## 题目

序列化二叉树的一种方法是使用前序遍历。当我们遇到一个非空节点时，我们可以记录下这个节点的值。如果它是一个空节点，我们可以使用一个标记值记录，例如 #。

		 _9_
		/   \
	   3     2
	  / \   / \
	 4   1  #  6
	/ \ / \   / \
	# # # #   # #

例如，上面的二叉树可以被序列化为字符串 "9,3,4,#,#,1,#,#,2,#,6,#,#"，其中 # 代表一个空节点。

给定一串以逗号分隔的序列，验证它是否是正确的二叉树的前序序列化。编写一个在不重构树的条件下的可行算法。

每个以逗号分隔的字符或为一个整数或为一个表示 null 指针的 '#' 。

你可以认为输入格式总是有效的，例如它永远不会包含两个连续的逗号，比如 "1,,3" 。

<!--more--> 

示例 1:

	输入: "9,3,4,#,#,1,#,#,2,#,6,#,#"
	输出: true
	
示例 2:

	输入: "1,#"
	输出: false
	
示例 3:

	输入: "9,#,#,1"
	输出: false


## 分析

想想怎么根据这个序列重构树，每次找最左边能插入子树的位置（'#' 代表空节点，不能插入），如果每次都能插入且最后不剩插入位置了就是正确的序列。

所以可以动态维护可插入位置的个数 cnt。初始 cnt=1，观察可知遇到数字并插入后 cnt+=1，遇到 '#' 并插入后 cnt-=1。
最后再判断 cnt 是否等于 0 即可。

## 解答

```python
def isValidSerialization(self, preorder: str) -> bool:
	cnt = 1
	for x in preorder.split(','):
		if cnt < 1:
			return False
		cnt += -1 if x == '#' else 1
	return cnt == 0
```

32 ms

